<Window x:Class="ObjectViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Object Viewer" Height="1080" Width="1920"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen">
    <Grid ShowGridLines="False">
        <Grid.RowDefinitions>
            <RowDefinition Height="20" />
            <RowDefinition />
            <RowDefinition Height="20" />
        </Grid.RowDefinitions>

        <Menu Grid.Row="0">
            <MenuItem Header="File">
                <MenuItem Header="Open" Click="OpenFile_Click" />
            </MenuItem>
            <MenuItem Header="Settings" Click="SettingsMenuItem_Click" />
        </Menu>
        
        <Image Grid.Row="1" Name="RenderImage" Stretch="Uniform" HorizontalAlignment="Stretch"
               VerticalAlignment="Stretch" Focusable="True" />

        <ProgressBar Grid.Row="2" x:Name="LoadingProgressBar" Visibility="Collapsed" />
    </Grid>
</Window>